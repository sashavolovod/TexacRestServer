create table orders
(
    order_id              bigint           not null        primary key,
    order_number          varchar(32),
    order_full_number     varchar(32),
    graph_id              bigint,
    graph_type_id         bigint,
    reason_order          bigint,
    order_date            timestamp,
    oboz_to               varchar(32),
    kod_to                bigint,
    item_no               varchar(32),
    detail_no             varchar(32),
    qty                   integer,
    real_qty              integer,
    customer_id           bigint,
    plan_date             timestamp,
    priority_id           bigint,
    plan_tech_date        timestamp,
    tech_date             timestamp,
    shop_date             timestamp,
    fact_ready_date       timestamp,
    plan_nv               float8,
    performer_shop        bigint,
    include_in_report     boolean,
    report_date           timestamp,
    technologist_id       bigint,
    locksmith_id          bigint,
    order_note            varchar(4096),
    fact_nv               float8,
    consignment_note      varchar(32),
    act_number            varchar(32),
    dest_store            varchar(32),
    another_time_ready    boolean,
    designer_id           bigint,
    unit_type_id          bigint,
    stopped               boolean,
    order_card            bigint,
    service_life_year     integer,
    service_life_day      integer,
    object_type           bigint,
    urgent                boolean,
    material_date         timestamp,
    spare                 boolean,
    resistance            integer,
    additional_order      boolean,
    classifier            varchar(16),
    price_date            timestamp,
    peo_date              timestamp,
    otiz_date             timestamp,
    weight                integer
);